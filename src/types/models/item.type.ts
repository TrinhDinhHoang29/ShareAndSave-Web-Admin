export interface IItem {
  id: number;
  categoryID: number;
  name: string;
  description: string;
  image: string;
  maxClaim: number;
  quantity: number;
  categoryName: string;
  createdAt: string;
}
